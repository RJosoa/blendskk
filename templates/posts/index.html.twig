<section class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 px-6 md:px-16 pb-10 md:pb-16 gap-5">
    {% for post in posts %}
        <div class="flex justify-center w-full">
            {{ include('posts/_card.html.twig', {
                'post': post,
                'likesRepository': likesRepository,
                'favoritesRepository': favoritesRepository
            }) }}
        </div>
    {% endfor %}
</section>
